{ "expect": "invalid", "expectedErrors": ["PACKAGE_ID_FORMAT"], "description": "package.id uses the wrong OCD domain (urn:ocd:project instead of urn:ocd:package). Identity domains are distinct on purpose: package != project != site." }
