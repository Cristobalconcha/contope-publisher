{ "expect": "invalid", "expectedErrors": ["DUPLICATE_REFERENCE", "ENTRYPOINT_ROLE_MISMATCH"], "description": "Two entrypoints alias the same file path (DUPLICATE_REFERENCE). Because each entrypoint key requires a distinct role and the aliased file has only one role, the second entrypoint also mismatches its expected role (ENTRYPOINT_ROLE_MISMATCH); both defects are real and reported together." }
