{ "expect": "valid", "description": "Positive coverage for the optional ContOpe Design Desktop interchange entrypoints (designModel, desktopSource, idml) and their roles, plus the pattern/navigation/fonts roles. Each entrypoint resolves to a file whose role matches. Distinct opaque package, project and site identities." }
